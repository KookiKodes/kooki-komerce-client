import initialCatState from './intialCategoryState';
import initialProdState from './initialCreateProductState';

const initialState = {
  category: initialCatState,
  products: [initialProdState],
};

export default initialState;
