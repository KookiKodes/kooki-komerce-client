import initialSubState from './intialSubCategoryState';
import initialProductState from './initialCreateProductState';

const initialState = {
  sub: initialSubState,
  products: [initialProductState],
};

export default initialState;
